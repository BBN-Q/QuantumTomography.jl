language: julia
julia:
  - 0.5
  - 0.6
  - 0.7
  - nightly
matrix:
  allowed_failures:
    - julia: nightly
    - julia: 0.7
notifications:
  email: false
# uncomment the following lines to override the default test script
#script:
#  - if [[ -a .git/shallow ]]; then git fetch --unshallow; fi
#  - julia --check-bounds=yes -e 'Pkg.clone(pwd()); Pkg.build("QuantumTomography"); Pkg.test("QuantumTomography"; coverage=true)'

# necessary to run on new container-based infrastructure
sudo: false
